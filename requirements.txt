# ── Core web framework ────────────────────────────────────────────────────────
Flask==3.0.3
flask-cors==4.0.1
flask-jwt-extended==4.6.0
flask-sqlalchemy==3.1.1
gunicorn==22.0.0

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.30
# Uncomment for PostgreSQL in production:
# psycopg2-binary==2.9.9

# ── AI / Computer Vision ──────────────────────────────────────────────────────
opencv-python-headless==4.9.0.80
numpy==1.26.4
# MediaPipe for advanced face-mesh and gaze detection
# Optional in cloud deployments (Streamlit/CI fallback to Haar detection when absent):
# mediapipe==0.10.14

# ── Utilities ─────────────────────────────────────────────────────────────────
Werkzeug==3.0.3
python-dotenv==1.0.1
# Streamlit Cloud deploys omit streamlit-option-menu/streamlit-webrtc to avoid unavailable pins.
# Python runtime is pinned via runtime.txt (3.11) to keep packages compatible.
streamlit==1.45.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0

streamlit-webrtc==0.47.0
streamlit-option-menu==0.3.9
opencv-python==4.8.1.78
mediapipe==0.10.9
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
tensorflow==2.13.0
Pillow==10.0.0
python-dotenv==1.0.0
SQLAlchemy==2.0.20
sqlalchemy-utils==0.41.1
pytz==2023.3
requests==2.31.0
pydantic==2.3.0
bcrypt==4.0.1
PyJWT==2.8.0
python-multipart==0.0.6
aiofiles==23.2.1
av==10.0.0
